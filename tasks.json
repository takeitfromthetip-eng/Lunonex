{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Build Everything",
			"type": "shell",
			"command": "npm run build backend && npm run build frontend && npm run build plugins",
			"isBackground": true,
			"group": {
				"kind": "build",
				"isDefault": true
			}
		}
	]
}